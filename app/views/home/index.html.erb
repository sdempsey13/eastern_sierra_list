<div class="sales-items-feed">
  <ul id="sale-items-list">
   <% @sale_item.each do |item| %>
     <li>
       <img><%= item.image %></img>
       <h3><%= item.title %></h3>
       <h4><%= item.location %></h4>
       <h4><%= item.price %></h4>
     </li>
   <% end %>
  </ul>
</div>
